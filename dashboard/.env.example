# Used for local dev + deployments (Vercel env var)
# Note: the dashboard currently uses a hard-coded API URL in code; this file is scaffolding
# for the upcoming env-driven API base URL wiring.
DASHBOARD_API_URL=http://localhost:3333/api
